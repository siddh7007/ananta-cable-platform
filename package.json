{
  "name": "cable-platform",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test || true",
    "start": "docker compose up --build",
    "sdk:gen": "./scripts/gen-sdk.sh",
    "contracts:verify": "./scripts/verify-contracts.sh"
  },
  "workspaces": [
    "apps/*",
    "services/*",
    "shared/*"
  ],
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}