{
  "name": "portal",
  "type": "module",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "lint": "eslint . --ext .ts,.js --no-error-on-unmatched-pattern",
    "test": "node -e \"console.log('Portal tests not configured')\""
  },
  "dependencies": {
    "@cable-platform/client-sdk": "workspace:*",
    "svelte": "^4.2.19"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "svelte-preprocess": "^6.0.3",
    "typescript": "^5",
    "vite": "^5"
  }
}
