{
  "name": "quality",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint . --ext .ts,.js --no-error-on-unmatched-pattern",
    "test": "node -e \"console.log('Quality tests not configured')\""
  }
}
