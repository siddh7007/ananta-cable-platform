{
  "name": "api-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint . --ext .ts,.js --no-error-on-unmatched-pattern",
    "test": "node -e \"console.log('API Gateway tests not configured')\""
  },
  "dependencies": {
    "@fastify/cors": "^9.0.0",
    "fastify": "^4.0.0",
    "jose": "^5.0.0"
  }
}
